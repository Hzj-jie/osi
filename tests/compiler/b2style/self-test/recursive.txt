
#include <b2style.h>
#include <testing.h>

int f(int x) {
  if (x < 100) {
    return f(x + 1);
  }
  return 100;
}

void main() {
  b2style::testing::assert_equal<int, int>(f(0), 100);
  b2style::testing::finished();
}